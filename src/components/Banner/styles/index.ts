import { baseClasses } from "./base.ts";
import { sizeClasses } from "./sizes.ts";
import { colorClasses } from "./colors.ts";

export const bannerStyles = {
  base: baseClasses,
  size: sizeClasses,
  color: colorClasses,
};
