import { timePickerRightSlotClasses } from "./base";
import { iconSizes } from "./sizes";

export const timePickerStyles = {
  rightSlot: timePickerRightSlotClasses,
  iconSize: iconSizes,
} as const;
