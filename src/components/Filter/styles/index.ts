import {
  bodyClasses,
  baseClasses,
  footerClasses,
  searchClasses,
  buttonClasses,
} from "./base";

export const filterStyles = {
  base: baseClasses,
  search: searchClasses,
  body: bodyClasses,
  footer: footerClasses,
  button: buttonClasses,
};
