export const imageWrapperClasses = "overflow-hidden rounded-scale-xl";

export const imageBaseClasses = "block w-full object-cover cursor-pointer";

export const imageClasses = "h-full";

export const imageGridClasses = "grid gap-2 w-full";

export const imageContainerClasses = "w-full flex items-center flex-col gap-2";

export const imageButtonAllClasses = "w-full";
