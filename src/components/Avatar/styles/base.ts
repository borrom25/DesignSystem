export const textClasses = `
  text-complementary
  font-roboto-flex
  not-italic
`;

export const baseClasses = `relative overflow-visible inline-flex items-center justify-center shrink-0 rounded-full text-complementary bg-generic-medium ${textClasses}`;

export const imageClasses = "w-full h-full object-cover rounded-full";

export const borderClasses = "border-2 border-brand-line-heavy";
