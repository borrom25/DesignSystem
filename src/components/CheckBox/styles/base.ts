import { formControlBaseClassesWithoutOutline } from "@/styles/shared";

export const baseClasses = `${formControlBaseClassesWithoutOutline} rounded-[5px] outline-none`;

export const labelClasses =
  "relative inline-flex items-center cursor-pointer has-[:disabled]:cursor-not-allowed";

export const containerClasses = "flex items-center gap-3";

export const titleClasses = "text-complementary";
