import { triggerBaseClasses, triggerButtonClasses } from "./base.ts";
import { triggerVariantsClasses } from "./variants.ts";

export const sidebarStyles = {
  triggerBase: triggerBaseClasses,
  triggerVariants: triggerVariantsClasses,
  triggerButton: triggerButtonClasses,
};
