import {
  datePickerRightSlotClasses,
  datePickerIconClasses,
  datePickerIconWrapperClasses,
  datePickerContainerClasses,
} from "./base";
import { iconSizes } from "./sizes";

export const datePickerStyles = {
  containerStyles: datePickerContainerClasses,
  iconStyles: datePickerIconClasses,
  iconWrapper: datePickerIconWrapperClasses,
  rightSlot: datePickerRightSlotClasses,
  size: iconSizes,
};
