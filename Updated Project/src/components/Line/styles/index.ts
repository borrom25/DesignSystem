import {
  baseClasses,
  contentClasses,
  headClasses,
  headColClasses,
  listClasses,
  subtitleClasses,
  titleClasses,
} from "./base";
import { positionClasses } from "./positions";
import { typeClasses } from "./types";

export const lineStyles = {
  base: baseClasses,
  type: typeClasses,
  position: positionClasses,
  list: listClasses,
  head: headClasses,
  headCol: headColClasses,
  content: contentClasses,
  title: titleClasses,
  subtitle: subtitleClasses,
};
