export { contentClasses, footerClasses } from "./base";

import { contentClasses, footerClasses } from "./base";

export const tableColumnsModalStyles = {
  content: contentClasses,
  footer: footerClasses,
} as const;
